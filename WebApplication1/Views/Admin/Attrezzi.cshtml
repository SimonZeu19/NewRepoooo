@model IEnumerable<WebApplication1.Models.Attrezzi>
@{
    ViewBag.Title = "Attrezzi";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<h2>Lista Atrrezzi</h2>

@*<table class="table table-striped table-hover">
    <tr>
        <th>@Html.DisplayNameFor(model => model.id_attrezzo)</th>
        <th>@Html.DisplayNameFor(model => model.nome)</th>
        <th>@Html.DisplayNameFor(model => model.marchio)</th>
        <th>@Html.DisplayNameFor(model => model.peso)</th>
        <th>@Html.DisplayNameFor(model => model.prezzo)</th>
        <th>@Html.DisplayNameFor(model => model.quantita)</th>

    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@Html.DisplayFor(Model => item.id_attrezzo)</td>
            <td>@Html.DisplayFor(Model => item.nome)</td>
            <td>@Html.DisplayFor(Model => item.marchio)</td>
            <td>@Html.DisplayFor(Model => item.peso)</td>
            <td>@Html.DisplayFor(Model => item.prezzo)</td>
            <td>@Html.DisplayFor(Model => item.quantita)</td>


        </tr>
    }
</table>*@

<div id="page-wrapper">
    <div class="main-page">
        <h2 class="title1">Hello, dear @Session["Nome"]</h2>
        <div class="blank-page widget-shadow scroll" id="style-2 div1">
            <div class="bs-example widget-shadow" data-example-id="hoverable-table">
                <h4>List of Cinema</h4>
                <br />
                <table class="table table-hover">
                    <thead> <tr><th>Cinema Name</th> <th>City</th> <th>Street</th> <th>Email</th> <th>Thelephone</th> <th>Construction Date</th> </tr> </thead>
                    <tbody>
                        @foreach (WebApplication1.Models.Attrezzi attrezzi in Model.listattrezzi)
                        {
                            <tr>
                                <td class="text-td-center">@attrezzi.listattrezzi</td>
                                @*<td class="text-td-center">@cinema.city</td>
                                <td class="text-td-center">@cinema.street</td>
                                <td class="text-td-center">@cinema.email</td>
                                <td class="text-td-center">@cinema.telephone_number</td>
                                @{ string onlyDate = cinema.building_date.ToShortDateString(); }*@
                                @*<td class="text-td-center">@onlyDate</td>*@
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>